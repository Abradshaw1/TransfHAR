# CNN backbone: model/encoding/objective/hparams for runs.
model: "CNN"
encoding: "raw"  # expects [B, C, T]
objective: "mae"  # placeholder; swap to consistency/supervised as needed
# hparams:
#   channels: [64, 128, 256]
#   kernel_sizes: [5, 5, 3]
#   dropout: 0.1
# optim:
#   lr: 3e-4
#   weight_decay: 0.01
