"""
TS-Transformer encoder placeholder (expects raw [B, C, T]).

Pseudocode:
- class TSTransformerEncoder(nn.Module):
    - __init__(cfg): define patch/embed + transformer blocks; produce embedding_dim
    - forward(x): returns embeddings
"""
